# ⚠️  IMPORTANT - Ne jamais committer de vraies clés dans ce fichier
# Ce fichier contient uniquement des valeurs factices pour la documentation

# === Configuration Supabase (clés publiques autorisées) ===
# Ces clés sont publiques par design et finissent dans le bundle client
VITE_SUPABASE_PROJECT_ID="votre-project-id"
VITE_SUPABASE_PUBLISHABLE_KEY="votre-anon-key-publique"
VITE_SUPABASE_URL="https://votre-project-id.supabase.co"

# === Où trouver ces valeurs ? ===
# 1. Dashboard Lovable Cloud : Paramètres > Backend
# 2. Dashboard Supabase : Project Settings > API

# === Secrets (JAMAIS dans .env, toujours dans Lovable Cloud Secrets) ===
# ❌ Ne JAMAIS ajouter ces variables ici ou dans .env :
#    - SUPABASE_SERVICE_ROLE_KEY
#    - Clés API tierces (OpenAI, Stripe, etc.)
#    - Tokens d'authentification
#    - Mots de passe
#
# ✅ Les ajouter via : Dashboard Lovable > Settings > Secrets
# ✅ Elles seront disponibles dans les Edge Functions via Deno.env.get()
